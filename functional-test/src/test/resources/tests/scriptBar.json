{
    "request": {
        "path": "/script/bar",
        "params" : [ {
            "name" : "query",
            "pattern" : "12090544"
        }]
    },
    "response": {
        "statusCode": 500,
        "body": "Sup?"
    },
    "script" : "if (request.params.get('run').value() == 'true') {response.statusCode = 200; response.body = request.params.get('run').value()}"
}
