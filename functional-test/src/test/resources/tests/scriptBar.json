{
    "request": {
        "path": "/script/bar"
    },
    "response": {
        "status": 500,
        "body": "Sup?"
    },
    "script" : "if (request.getParam('run') == 'true') { response.statusCode = 200; response.body = request.getParam('run'); }"
}
